This directory contains assembler and disassembler support for the ARMv6-M
architecture. Currently this means the Cortex-M0 and -M0+ processors.

But note that since the v6-M architecture is a _strict subset_ of v7-M, v7-M
processors (Cortex-M3 and -M4(F)) will load this code as well.
