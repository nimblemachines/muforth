General muforth to do list.

** change cell to intptr_t? this relates to 32/64 bit stuff as well.

** if not intptr_t choose a stdint to get a specific cell size.

** change mp-math to use uint16_t (C99 std) rather than u_int16_t.


