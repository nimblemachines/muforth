This directory contains assembler and disassembler support for the ARMv7-M
architecture. Currently this means the Cortex-M3 and -M4(F) processors.

But note that since the v6-M architecture is a _strict subset_ of v7-M, v7-M
processors will load that code as well.
